# задание c_2 Программа «Таблица перевода температур»

## Задача - вывод таблицы температур по Фаренгейту и перевод их по Цесльсию

Создать:

    - main.c — файл с кодом;

    - Makefile — исправить TARGET = c_2

Запустить сборку:

    make

Файл программы c_2.exe создатся в директории bin

Запустить программу:

    ./bin/c_2.exe

Проверить результат вывода и потом очистить проект в консоли VS Code:

    make clean

### Часть первая

В консоли должна появиться таблица из двух столбцов в таком виде:

    0   -17
    20  -6
    ...
    300  148

первый столбец Фаренгейт с шагом 20

втрой столбец перевод в Цельсия.

В Makefile исправить:

    SRC_FILES = main_1.c

### Часть вторая

Переделать первый вариант так, чтобы использовались вещественные числа с одной цифрой после десятичной точки
и выравниванием цифр по правому краю.

В Makefile исправить:

    SRC_FILES = main_2.c

в консоли должна появиться таблица из двух столбцов в таком виде:

    0   -17.8
    20   -6.7
    ...
---
Очистить проект в консоли VS Code:

    make clean

### Часть третья

Задача - вывод таблицы температур по Цельсию и перевод их в Фаренгейт

Вывести заголовок таблицы

В Makefile исправить:

    SRC_FILES = main_3.c

### Часть четвёртая

Задача - использование цикла for()

Переделать программу, сократить кол-во операторов за счёт использования другой конструкции цикла.

В Makefile исправить:

    SRC_FILES = main_4.c

Собрать и запустить:

    make
    ./bin/c_2.exe

Проверить вывод в терминале:

    TABLE OF TEMPERATURE
    0   -17
    20  -6
    ...
    300  148
---
Очистить проект в консоли VS Code:

    make clean

### Часть пятая

Задача - вывести таблицу в обратном порядке от 300 до 0 Фаренгейт

В Makefile исправить:

    SRC_FILES = main_5.c

Собрать и запустить:

    make
    ./bin/c_2.exe

В консоли должна появиться таблица из двух столбцов в таком виде:

    TABLE OF TEMPERATURE
    300  148
    ...
    20  -6
    0   -17
---
Очистить проект в консоли VS Code:

    make clean

### Часть шестая

Задача - использовать константы для постоянных числовых значений температуры  по Фаренгейту - нижний предел 0, верхний предел 300, шаг 20

В Makefile исправить:

    SRC_FILES = main_6.c

Собрать и запустить:

    make
    ./bin/c_2.exe

Проверить вывод в терминале:

    TABLE OF TEMPERATURE
    0   -17
    20  -6
    ...
    300  148
---
Очистить проект в консоли VS Code:

    make clean
